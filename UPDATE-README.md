# Estra7ah program updates

Runtime program files only. User databases, settings, media, backups and signing private keys are excluded.

The updater verifies the signed release manifest and downloads changed files from its exact commit.
